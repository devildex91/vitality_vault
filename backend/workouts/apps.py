"""Django application configuration for workout data and APIs."""

from django.apps import AppConfig


class WorkoutsConfig(AppConfig):
    """Configure the workouts Django application."""

    name = "workouts"
